export default function Loading() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-[#050505]">
      <div className="font-mono text-[10px] uppercase tracking-[0.3em] text-white/40 animate-pulse">
        Chargement — FIFA26 / Control
      </div>
    </div>
  )
}
